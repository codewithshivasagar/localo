import { Button, Card } from "@localo/ui";

export default function Page() {
  return (
    <main style={{ minHeight: "100vh", padding: 32 }}>
      <Card>
        <h1>Localo Partner</h1>
        <p>Localo starter app is ready.</p>
        <Button>Get Started</Button>
      </Card>
    </main>
  );
}
